import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webcodestudio.mobile',
  appName: 'web-code-app-mobile',
  webDir: 'www'
};

export default config;
